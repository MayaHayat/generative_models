# excluded: large third-party dataset clone, not "the code"
covid-chestxray-dataset/

# excluded: separate untouched project with its own local edits
medSynthesisV1-generative_models/

# generated data / run artifacts across all sub-projects
**/data/
**/runs/
**/_smoketest_*/

__pycache__/
*.pyc
.ipynb_checkpoints/

# Reference paper PDFs — third-party published works, kept locally only
covid gans.pdf
Medical Image Synthesis with Context-Aware.pdf

# Course/assignment instructions — not part of the submission
FinalProjectPart2.pdf

# Personal research notes (paper-selection survey) — not part of the submission
GAN_Medical_Imaging_Paper_Survey.md

# Trained model weights — too large for git, regenerate with train_gan.py
**/weights/

# macOS
.DS_Store

# Claude Code local state (includes a full worktree checkout)
.claude/

# IDE
.idea/

# Submission branch carries code only. Reports, findings and READMEs are
# delivered separately (PDF/Overleaf) and remain on disk and on the
# analysis branches. Drop this rule to track markdown again.
*.md
# ...except the READMEs, which document the code and do ship.
!README.md

# Report LaTeX sources ship separately, like the markdown above.
*.tex

# Report bundle leftovers: Overleaf figure copies (duplicates of
# stage2/results/figures/) and the report text dump.
CovidGAN-Pytorch/docs/
